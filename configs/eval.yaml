# @package _global_

defaults:
  - _self_
  - data: cub.yaml # choose datamodule with `test_dataloader()` for evaluation
  - model:  classification_prototype.yaml
  - logger: tensorboard
  - trainer: default.yaml
  - paths: default.yaml
  - extras: default.yaml
  - hydra: default.yaml

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: /home/WRS/ProtosViT/logs/train/neurips/cub_baseline_dino_cub/checkpoints/epoch_074.ckpt
